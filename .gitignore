# Dependencies
node_modules

# Build output
dist

# Generated
src/convex/_generated

# Environment / secrets
# `.env.example` is intentionally tracked; every other env file is not.
.env
.env.*
!.env.example

# Credentials
*.pem
*.key
*.p12
*.keystore
*.jks
.netrc
.git-credentials

# Local tooling noise
.DS_Store
*.log
*.bak

# Phase 179 — mobile signing material. NEVER commit these.
android/keystore.properties
*.jks
*.keystore
*.mobileprovision
*.p12
*.cer
ios/App/Pods/

# Phase 182 — desktop (Tauri) build output and generated files.
src-tauri/target/
src-tauri/gen/
# Windows signing material. NEVER commit these.
*.pfx
*.snk

# Phase 193 — regenerated by the orphan-key guard on every run; evidence is
# reproducible from scripts/i18n-key-usage-report.mjs, so committing it would
# only create churn.
docs/i18n-key-usage.json
docs/i18n-key-classification.json
docs/component-debt.json
docs/component-debt-table.md

# Phase 265 — mobile web assets sync from dist/index.html (gitignored, reproducible via test:release)
android/app/src/main/assets/public/
ios/App/App/public/
