# ─────────────────────────────────────────────────────────────
# Copy to `.env.local` and fill in. Never commit real values.
# ─────────────────────────────────────────────────────────────

# Convex deployment URL - replace with your actual deployment URL
# Run 'npx convex dev' to get the development URL
VITE_CONVEX_URL=

# Convex site URL for auth
CONVEX_SITE_URL=http://localhost:5173

# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=

# ── Auth: Xstarz-owned OTP email delivery ────────────────────
# SERVER-ONLY. Set these in the Convex deployment environment
# (`npx convex env set ...`), never in the client bundle and never here.
#
# Phase 185 replaced third-party OTP delivery with an Xstarz-owned
# transport. Changing provider is a configuration change only.

# Which transport to use: resend | smtp2go | console
# `console` is for local development: it delivers nothing and needs no key.
XSTARZ_EMAIL_TRANSPORT=console

# API key for the selected transport. Required unless transport=console.
XSTARZ_EMAIL_API_KEY=

# Sender identity. Must be a domain Xstarz owns and has verified with the
# provider. There is no default: production refuses to send without it,
# rather than falling back to a sender the project does not control.
XSTARZ_EMAIL_SENDER_ADDRESS=
XSTARZ_EMAIL_SENDER_NAME=Xstarz Analysis

# Optional. Milliseconds before a delivery attempt is abandoned (default 10000).
XSTARZ_EMAIL_TIMEOUT_MS=

# Optional. Federated sign-in from the legacy hosting platform. Leave EMPTY in
# production: setting it makes this deployment trust an external issuer, so any
# party controlling that issuer's JWKS could mint accepted identities.
VLY_CONVEX_AUTH_ISSUER=

# REMOVED in Phase 185:
#   OTP_EMAIL_API_KEY  - credential for the retired third-party OTP service.
#                        Still compromised in Git history; Phase 184 owns
#                        rotation and history remediation.
#   VLY_APP_NAME       - existed only to label that service's email.

# ── Market data providers ────────────────────────────────────
# All optional. A missing key disables that provider explicitly; it never
# causes fabricated or placeholder market data.
TWELVE_DATA_API_KEY=
ALPHA_VANTAGE_API_KEY=
COINGLASS_API_KEY=
TICKATLAS_API_KEY=
EIA_API_KEY=

# OKX, CoinGecko, DeFiLlama, Tokenomist, CFTC and US Treasury are public
# endpoints and require no credentials.
