{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the Xstarz Analysis desktop shell. Every permission here is justified; nothing grants the web layer filesystem, shell, process or network-bypass access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
